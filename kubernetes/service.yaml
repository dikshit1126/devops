apiVersion: v1
kind: Service
metadata:
  name: showcase-service
  namespace: devops-showcase
spec:
  selector:
    app: showcase-app
  ports:
    - port: 80
      targetPort: 3000
  type: LoadBalancer
